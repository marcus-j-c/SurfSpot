info:
  name: randomBeachTest
  type: http
  seq: 14

http:
  method: GET
  url: "{{baseURL}}/beaches?name=supertubos-beach"
  params:
    - name: name
      value: supertubos-beach
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
