info:
  name: unknownTest
  type: http
  seq: 11

http:
  method: GET
  url: "{{baseURL}}/beaches?name=faslkdfhaskdfh"
  params:
    - name: name
      value: faslkdfhaskdfh
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
