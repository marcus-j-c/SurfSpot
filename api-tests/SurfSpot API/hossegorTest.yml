info:
  name: hossegorTest
  type: http
  seq: 12

http:
  method: GET
  url: "{{baseURL}}/beaches?name=hossegor"
  params:
    - name: name
      value: hossegor
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
