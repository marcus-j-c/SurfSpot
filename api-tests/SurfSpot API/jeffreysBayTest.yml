info:
  name: jeffreysBayTest
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseURL}}/beaches?name=jeffreys-bay"
  params:
    - name: name
      value: jeffreys-bay
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
