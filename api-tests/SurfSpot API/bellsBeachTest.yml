info:
  name: bellsBeachTest
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseURL}}/beach/bells-beach"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
