info:
  name: nazaréTest
  type: http
  seq: 6

http:
  method: GET
  url: "{{baseURL}}/beaches?name=nazaré"
  params:
    - name: name
      value: nazaré
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
