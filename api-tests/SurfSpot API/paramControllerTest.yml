info:
  name: paramControllerTest
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseURL}}/waterTemp?water=mediteranian"
  params:
    - name: water
      value: mediteranian
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
