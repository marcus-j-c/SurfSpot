info:
  name: randomBeachTest
  type: http
  seq: 12

http:
  method: GET
  url: "{{baseURL}}/beaches?name=fistral"
  params:
    - name: name
      value: fistral
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
