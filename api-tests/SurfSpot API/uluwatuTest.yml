info:
  name: uluwatuTest
  type: http
  seq: 9

http:
  method: GET
  url: "{{baseURL}}/beaches?name=uluwatu"
  params:
    - name: name
      value: uluwatu
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
