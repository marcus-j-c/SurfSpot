info:
  name: banzaiPipelineTest
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseURL}}/beach/BanzaiPipeline"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
