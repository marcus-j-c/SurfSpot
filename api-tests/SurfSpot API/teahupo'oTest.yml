info:
  name: teahupo'oTest
  type: http
  seq: 6

http:
  method: GET
  url: "{{baseURL}}/beaches?name=teahupo'o"
  params:
    - name: name
      value: teahupo'o
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
