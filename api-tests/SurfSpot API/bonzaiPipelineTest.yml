info:
  name: bonzaiPipelineTest
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseURL}}/beaches?name=bonzai-pipeline"
  params:
    - name: name
      value: bonzai-pipeline
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
