info:
  name: goldCoastTest
  type: http
  seq: 8

http:
  method: GET
  url: "{{baseURL}}/beaches?name=gold-coast"
  params:
    - name: name
      value: gold-coast
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
