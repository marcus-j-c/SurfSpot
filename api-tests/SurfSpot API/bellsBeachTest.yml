info:
  name: bellsBeachTest
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseURL}}/beaches?name=bells-beach"
  params:
    - name: name
      value: bells-beach
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
