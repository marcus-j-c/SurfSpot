info:
  name: mavericksTest
  type: http
  seq: 9

http:
  method: GET
  url: "{{baseURL}}/beaches?name=mavericks"
  params:
    - name: name
      value: mavericks
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
