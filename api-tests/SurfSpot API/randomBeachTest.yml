info:
  name: randomBeachTest
  type: http
  seq: 14

http:
  method: GET
  url: "{{baseURL}}/beaches?name=teahupo'o-beach-spot"
  params:
    - name: name
      value: teahupo'o-beach-spot
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
