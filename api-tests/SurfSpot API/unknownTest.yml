info:
  name: unknownTest
  type: http
  seq: 13

http:
  method: GET
  url: "{{baseURL}}/beaches?name=gibberish"
  params:
    - name: name
      value: gibberish
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
