info:
  name: supertubosTest
  type: http
  seq: 7

http:
  method: GET
  url: "{{baseURL}}/beaches?name=supertubos"
  params:
    - name: name
      value: supertubos
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
