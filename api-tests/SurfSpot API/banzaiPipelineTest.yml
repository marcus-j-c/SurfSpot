info:
  name: banzaiPipelineTest
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseURL}}/beaches?name=banzai-pipeline"
  params:
    - name: name
      value: banzai-pipeline
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
